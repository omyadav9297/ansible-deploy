- hosts: all
  become: yes

  tasks:
    - name: install apache httpd
      yum:
        name: httpd
        state: present

    - name: start and enable apache service
      service:
        name: httpd
        state: started
        enabled: yes